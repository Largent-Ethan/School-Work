How to run the program

To compile the program, type the following into the command line:
    gcc --std=gnu99 myCounter.c -lpthread - o myCounter


To run the program, type the following into the command line:
    ./myCounter